# Backlog — M1: Feel ("one good car") + ADR bridge

> The **active** milestone, decomposed into ~1-hour pieces — one a day keeps the project
> alive. The milestone goal + checkpoint live in [ROADMAP.md](../ROADMAP.md); this is just the
> day-queue.
>
> **One backlog per milestone, one alive at a time.** When this milestone passes its fun
> checkpoint, this file is done — freeze or delete it and create `M2.md` for the next one.
>
> **Tags:** `[code] [art] [audio] [design]`. ⚡ = quick win (<30 min, low-energy day).
> **Dep:** needs another task first. **Free:** grab any day — good for switching task *type*
> when you don't feel like coding.

---

## 🌉 ADR bridge — decide before M1

### ADR-001 — Physics model ✅
- [x] `[design]` **Spike A: VehicleBody3D** — drove it; chosen base.
- [x] `[design]` **Spike B: arcade raycast car** — drove it; reference only, not adopted.
- [x] `[design]` **Write ADR-001** — accepted: VehicleBody3D + thin custom grip/handling layer.
      Also validated 3-surface grip + drift in the Spike A demo.

### ADR-002 — Track representation
- [ ] `[design]` **Spike: ribbon road** — `Path3D` + `CSGPolygon3D` extruded along a curve,
      drive the M1 car on it. ~1h
- [ ] `[design]` **Spike: sample curve points** — pull ordered points off the `Curve3D`
      (proves checkpoints + AI line are cheap later). ~1h
- [ ] `[design]` **Write ADR-002** — road repr, collision source, how AI line/checkpoints
      bake from the spline. Dep: both spikes. ~1h

---

## M1 — Feel ("one good car")

### Test track (do early — everything drives on it)
- [ ] `[code]` Gray-box loop: lay a `Path3D` curve + spawn point + ground plane. Dep: ADR-002. ~1h
- [ ] `[code]` Extrude road ribbon mesh + add collision to it. Dep: above. ~1h
- [ ] `[code]` ⚡ Drop a few reference markers (start/finish posts) to read speed/turns. Free. <30m

### Vehicle controller (per ADR-001)
- [ ] `[code]` Ground/suspension raycasts + keep car at ride height. Dep: ADR-001. ~1h
- [ ] `[code]` Throttle + brake along forward axis. Dep: above. ~1h
- [ ] `[code]` Steering + turn response. Dep: throttle. ~1h
- [ ] `[code]` Lateral grip / friction model (the core of feel). Dep: steering. ~1h
- [ ] `[code]` Handbrake. Dep: grip. ~1h
- [ ] `[code]` ⚡ Reset-car action (respawn at last spawn). Free-ish. <30m
- [ ] `[code]` Feel-tuning pass: tune accel/grip/steer constants until buttery. Iterative. ~1h
- [ ] `[code]` Second feel-tuning pass after a break (fresh eyes). ~1h

### Camera
- [ ] `[code]` Chase camera that follows the car. Free (parallel to controller). ~1h
- [ ] `[code]` Camera lag/weight + look-ahead into turns. Dep: above. ~1h
- [ ] `[code]` ⚡ Speed-based FOV kick (subtle). Dep: camera. <30m

### Placeholder art + audio (grab on non-code days)
- [ ] `[art]` Block out a placeholder car in MagicaVoxel. Free. ~1h
- [ ] `[art]` Export + import the voxel car, parent it to the controller. Dep: car block-out. ~1h
- [ ] `[art]` ⚡ Simple skybox / ground material so the gray-box reads. Free. <30m
- [ ] `[audio]` Find a CC0 engine loop, wire pitch to throttle. Free. ~1h
- [ ] `[audio]` ⚡ CC0 tire-screech placeholder on low grip. Free. <30m

### Juice (quick wins for bad days)
- [ ] `[code]` ⚡ Tiny screen-shake on hard landings. Free. <30m
- [ ] `[code]` ⚡ Skid-mark decal or dust puff at low grip. Free. <30m

### 🚧 M1 Checkpoint
- [ ] `[design]` Play it, read through FUN_CHECKLIST (feel/flow), write the **fun verdict**
      in PLAYTESTS.md. "Not fun" → iterate, don't advance. ~1h
